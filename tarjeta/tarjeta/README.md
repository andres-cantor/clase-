# card

![alt text](image.png)

# DecMarket

# Props
-prop = property
function saludar(nombre){
   return "Hola" + nombre;
}

<Navbar brand="Devmarket" />
--objeto

# Brand
-> desestructuración en react
-const {brand} = props;


- Repositorio = carpeta controlada por Git
- commit = snapshot del proyecto
- Rama(branch) = linea de trabajo
  - local
  - internet
  - repo

  # git init
  - arrancar un git en el folder

  # git clone
   - npm i 
   
# estado de los archivos 
   - git status

# git add
- git add . -> agrega todo
- git add archivo.txt -> agrega un archivo

# git commit
- Alias -> git commit -m "mensaje"

# git push 
- git push origin main -u feature_Ejemplo
- git push origin feature_ejemplo

- main-> estable
   - develop -> desarrollo
   -  hijos -> features
      - develop/view-login
 # git branch
 - crear una rama

 # git checkout nombreRama
 - moverse a una rama 

 # git branch -d nombre-rama

# git fetch
- bajar ramas del repo 

# Mezclar ramas (Merge)
- git merge nombre-rama 
   - git add . + git commi
   - develop -> git pull - baja lo que tiene el repo
   - van a su rama y hace git merge develop

-Resolver de conflictos

<<<<<<HEAD entrantes - salientes -locales - rechazar 
tuversion

=========

version de otra rama

>>>>>>>> rama-objetivo

# git log 


